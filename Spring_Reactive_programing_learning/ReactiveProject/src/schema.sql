CREATE table mydb.book(
                   book_id INT(11) Primary key auto_increment,
                   name varchar(255),
                   book_desc Text,
                   publisher varchar(255),
                   author varchar(255)
)